# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'oyuncak_takasi' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for oyuncak_takasi
  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  pod 'Firebase/Firestore'
  pod 'Firebase/Messaging'
  pod 'Firebase/Database'
  pod 'Firebase/Storage'

  pod 'Geolocator', '9.0.2'
  pod 'FlutterLocalNotifications', '14.1.1'

  # Diğer CocoaPods bağımlılıklarınızı buraya ekleyin

  # Flutter için gerekli olan CocoaPods bağımlılıkları
  pod 'Flutter'

end
